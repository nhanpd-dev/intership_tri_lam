import styled from 'styled-components';

import COLORS from '../../../../../themes/colors';

export const ProvisionalCalculation = styled.div`
  .ant-divider-horizontal {
    margin: 12px 0;
  }

  .red-color {
    color: ${COLORS.red};
  }

  .title_payment {
    font-weight: 300;
    color: #333333;
  }
`;
